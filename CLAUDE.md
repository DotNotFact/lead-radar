# Lead Radar

Персональная система поиска заказов и управления действиями для .NET-разработчика
(фриланс + удалённая работа). Работает на личном ПК владельца, единственный пользователь — владелец.

Три задачи в порядке важности:
1. Не пропустить релевантный заказ — уведомление в течение минуты после публикации.
2. Не дать забыть про важное — ежедневная очередь действий с эскалацией просроченного.
3. Накопить данные о рынке для анализа каналов/ниш/ценников через 3–4 недели.

## ЖЁСТКИЕ ИНВАРИАНТЫ

Не обсуждаются и не нарушаются ни при каких оптимизациях.

1. **Никаких автоматических откликов, сообщений и комментариев.** Система только читает и уведомляет.
   Любой исходящий контакт с заказчиком делает человек руками.
2. **Секреты только в `.env`.** `.env`, `*.session`, `*.db` — в `.gitignore` с первого коммита.
   Никогда не логировать значения токенов, ключей, session-строк.
3. **Telegram-аккаунт коллектора — отдельный, не основной.** Только чтение. Не вступать в чаты
   программно, не менять профиль, не отправлять ничего от имени юзербота.
4. **Вежливый парсинг.** Для источников без API: не чаще 1 запроса в 10 секунд на домен, честный
   User-Agent с контактом, уважать `robots.txt`, кэшировать, не долбить при 429/403 — экспоненциальный
   бэкофф и тихий режим на час.
5. **Экспорт всегда обезличен.** Из выгрузки вырезаются user_id, телефоны, email, @username, имена
   собственные. Hard-требование функции экспорта, не опция.
6. **Деградация вместо падения.** Смерть одного источника не роняет систему. Источник помечается
   `degraded`, владелец получает уведомление, остальное работает.
7. **Никаких обходов защиты.** Не обходить капчи, не подделывать сессии, не использовать чужие
   аккаунты, не парсить площадки, где это прямо запрещено и технически заблокировано. Если источник
   не даётся — ручной режим, а не война с ним.

## ТРЕБОВАНИЯ К КОДУ

- Type hints везде, `mypy --strict` проходит.
- Никакой бизнес-логики в обработчиках бота — только вызов сервисов.
- Все внешние вызовы обёрнуты в таймауты и retry с экспоненциальным бэкоффом.
- Структурное логирование в файл с ротацией, отдельный уровень/событие для «источник сломался».
- Конфигурация — только через `.env` и YAML, никаких констант в коде.
- Каждая фаза заканчивается рабочим состоянием: система не должна быть сломана между фазами.
- Комментарии и коммиты — на русском.

## СТЕК

Python 3.13 (в промте указан 3.12, на машине владельца установлен только 3.13 — используем его,
совместимость со всем стеком ниже сохраняется), Telethon, aiogram 3.x, httpx + selectolax,
feedparser, SQLite + aiosqlite, APScheduler, pydantic-settings, pytest + pytest-asyncio.

## ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

- **Фаза 0 (фундамент) — выполнена.** Структура проекта, конфиги (`sources.yaml`, `keywords.yaml`,
  `actions.yaml`), `.env.example`, миграции БД, модели, слой БД, структурное логирование, тесты.
- **Фаза 1 (hh.ru) — выполнена.** `src/collectors/hh_ru.py` (пагинация, дедуп по id, полное
  описание вакансии вторым запросом), `src/scoring/{budget.py,scorer.py,dedup.py}`,
  `src/core/{yaml_config.py,repository.py,http.py}` (общий ретрай с бэкоффом на 429/5xx),
  `scripts/collect_hh.py` для разового прогона. 30 тестов, `mypy --strict` чист.
  **Важно:** `api.hh.ru` отдаёт 403 (DDoS-Guard) с IP песочницы разработки — обычный интернет
  при этом работает (проверено на example.com и hh.ru/robots.txt). Живой гейт "100+ реальных
  вакансий в БД" не пройден из песочницы принципиально (обходить защиту — нарушение инварианта 7).
  Заменён интеграционным тестом (`tests/test_collect_hh_pipeline.py`), гоняющим тот же объём и
  форму данных через весь конвейер на моках. **Владельцу нужно один раз запустить
  `python -m scripts.collect_hh` на своей машине**, чтобы наполнить БД реальными вакансиями —
  оттуда `api.hh.ru`, скорее всего, ответит нормально.
- **Фаза 2 (Telegram + бот + уведомления) — выполнена (код и тесты; живая проверка не сделана,
  см. ниже).** `src/collectors/telegram.py` (Telethon-клиент внедряется снаружи через Protocol —
  тестируется без реальной сессии; catch-up через `fetch()`, реалтайм через
  `register_realtime_handler()`), `src/notifier/{formatter.py,keyboard.py,notifier.py}`
  (формат сообщения из ТЗ, инлайн-кнопки Открыть/Ответил/Мимо), `src/control/bot.py`
  (aiogram-роутер: `/health`, `/sources`, `/pause`, `/resume`, `/addchat`, обработчик кнопок
  исхода лида), `src/control/chat_config.py` (чаты из /addchat хранятся отдельно от
  sources.yaml, чтобы не терять комментарии владельца при перезаписи), миграция
  `0002_system_state.sql`. 46 тестов, `mypy --strict` чист.
  **Не проверено вживую и не может быть проверено из песочницы:**
  - `api.telegram.org` не резолвится/не коннектится из этой машины (сетевая ошибка, не 403) —
    даже `getMe` с реальным токеном бота не прошёл. Обычный интернет при этом работает
    (см. Фазу 1). Значит live-тест бота и Telethon-сессии возможен только на машине владельца.
  - Живая сессия Telethon требует интерактивного ввода кода из СМС на настоящий телефон —
    в принципе не может быть создана автоматически, только владельцем руками.
  - Ни одно сообщение не было отправлено ни в какой чат/канал за время работы — отправка
    сообщений требует явного разрешения владельца в чате и реального `CHANNEL_ID`, которого
    нет.
  **Что нужно от владельца, чтобы включить эту фазу живьём:**
  1. `TELEGRAM_API_ID` / `TELEGRAM_API_HASH` с my.telegram.org (для отдельной симки) → в `.env`.
  2. Первый запуск Telethon-клиента на своей машине (один раз ввести код из Telegram) —
     появится `*.session` файл, он в `.gitignore`.
  3. Создать приватный канал, добавить туда бота-администратором, вписать `CHANNEL_ID` в `.env`.
  4. Заполнить `config/sources.yaml` → `telegram.chats` (60–100 чатов) и поставить
     `enabled: true`.
  Полная связка (планировщик + Telethon realtime + polling бота) будет собрана в `src/main.py`
  в Фазе 3 вместе с APScheduler для ежедневного брифа — тогда же будет что реально запускать
  одной командой.
- **Фаза 3 (модуль действий, бриф, планировщик) — выполнена.** `src/actions/materializer.py`
  (превращает шаблоны `actions.yaml` в строки `actions` идемпотентно: daily/every_3_days/weekly,
  сезонные по `MM-DD` раз в год, разовые — один раз навсегда), `src/actions/brief.py` (дайджест:
  действия на сегодня, просроченное с эскалацией 🔴 при `snooze_count >= 3` и `expected_value`,
  итоги суток, здоровье источников). Бот-команды `/brief`, `/todo`, `/done`, `/snooze` в
  `src/control/bot.py`. **`src/main.py`** — единая точка входа: APScheduler крутит опрос hh.ru
  по расписанию, отправку уведомлений (раз в минуту), ежедневный бриф (cron по
  `DAILY_BRIEF_TIME`); Telegram catch-up + realtime поднимаются, если заполнены
  `TELEGRAM_API_ID/HASH`; polling бота. Отсутствие `CHANNEL_ID` не роняет процесс — просто не
  шлёт уведомления/бриф (инвариант 6). 63 теста, `mypy --strict` чист.
  **Не проверено вживую** (по тем же причинам, что и Фаза 2: нет сети до `api.telegram.org`
  из песочницы, нет `CHANNEL_ID`, нет реальной Telethon-сессии) — `src/main.py` только
  импортирован и проверен на отсутствие ошибок инициализации модулей, реальный `python -m
  src.main` нужно запустить на машине владельца после заполнения `.env`.
- **Фаза 4 (Tier 2/1: Kwork, FL.ru, Хабр Фриланс, RSS) — выполнена, с осознанными
  архитектурными решениями по robots.txt.**
  - **Kwork — два отдельных источника**, а не один: живой раздел заказчиков и каталог услуг
    оказались двумя разными страницами с разным смыслом. `kwork_projects` (`src/collectors/
    kwork.py: KworkProjectsCollector`) читает `kwork.ru/projects` (раздел проектов заказчиков,
    доступен без авторизации) — это настоящие лиды, идут через обычный скоринг. `kwork_catalog`
    (`KworkCatalogCollector`) читает каталог услуг фрилансеров по категориям — это **не лиды**,
    а рыночная аналитика (title, price, seller_level, delivery_days, кол-во отзывов как прокси
    orders_completed — отдельного счётчика продаж на карточке нет), пишется с `score=None`,
    что технически гарантирует (`NULL >= порог` всегда ложь в SQL) — никогда не уведомляется.
    Оба парсят встроенный в HTML `window.stateData=` JSON (сервер-рендер), а не CSS-селекторы —
    надёжнее к вёрстке, но всё ещё "селектор" в смысле ТЗ: при смене структуры бросается
    `SourceUnavailableError` → источник уходит в degraded с понятной причиной.
    **robots.txt kwork.ru (`User-agent: *`) запрещает `/*?page=` и `/*&page=`** — поэтому оба
    коллектора берут только первую страницу за опрос. Это соблюдение robots.txt, а не баг.
  - **FL.ru переведён в Tier 3 (ручной режим), не Tier 2.** robots.txt (`User-agent: *`) прямо
    запрещает `/catalog*` (каталог проектов — как раз то, что нужно парсить) и `*/rss/*` (RSS
    тоже запрещён). Автоматический сбор сознательно не реализован — инвариант 7 запрещает
    обходить защиту, источник, который не даётся, переводится в ручной режим.
  - **Хабр Фриланс переведён в Tier 3.** robots.txt: `User-agent: *` → `Disallow: /` — запрещён
    весь сайт целиком. Автоматический сбор не реализован и не будет, пока сайт не изменит это
    правило.
  - **`rss_remote_jobs` (Tier 1) заполнен.** Проверено вручную 2026-08-17: We Work Remotely
    (`weworkremotely.com/categories/remote-programming-jobs.rss`) — живой, валидный RSS 2.0,
    robots.txt разрешает. RemoteOK RSS-эндпоинты отдают 410 Gone (RSS у них отключён, есть
    только JSON `/api` — вне рамок feedparser-коллектора), отмечено `disabled: true` в
    `sources.yaml` с причиной, не удалено.
  - Общий хвост "распарсить бюджет → оценить → дедуп по хешу → сохранить" вынесен в
    `src/core/pipeline.py` (`score_and_store_lead`) — раньше был скопирован в hh.ru, Kwork,
    Telegram-путь `main.py`; теперь один код на все источники с реальными лидами.
  - Все новые джобы (hh.ru, RSS, Kwork projects/catalog) добавлены в планировщик `src/main.py`.
  75 тестов, `mypy --strict` чист.

  **Проверено вживую с этой машины** (в отличие от Фаз 1-3 — `kwork.ru` и `weworkremotely.com`
  не блокируются, в отличие от `api.hh.ru`/`api.telegram.org`): `python -m scripts.collect_kwork`
  реально забрал 12 лидов с `kwork.ru/projects` и 72 записи каталога (3 категории × 24) —
  проверены budget/score в БД, у каталога `score IS NULL` как задумано. `python -m
  scripts.collect_rss` реально сходил на живой фид, распарсил 25 записей, `since`-фильтр
  корректно отсеял устаревшие (последняя публикация в фиде была за ~26 дней до прогона).
  Живой прогон нашёл и исправил два реальных бага, не всплывших на моках:
  1. **`stateData.kworks.posts.data` оказался неверным путём** — реальная структура на
     20+ минут более глубокая: `stateData.viewData.kworks.posts.data`. Собственная
     smoke-проверка коллектора поймала это на первом же живом запуске (не упала — источник
     корректно ушёл в degraded с понятной причиной), баг исправлен, фикстуры тестов обновлены.
  2. **`Settings` падал на пустых `TELEGRAM_API_ID=`/`CHANNEL_ID=`** в `.env` (именно так они
     стоят до заполнения владельцем) — pydantic пытался распарсить `""` как `int` и ронял весь
     процесс при любом запуске. Добавлен `field_validator` (пустая строка → `None`), тест на
     регресс в `test_config.py`.
- **Фаза 5 (экспорт, статистика, устойчивость, автозапуск) — выполнена.** Все фазы плана
  закрыты.
  - `src/export/anonymizer.py` — вырезает телефоны (РФ/международные, эвристика по группировке
    цифр 3-3-2-2, отличающая номера от бюджетных чисел и ISO-дат), email, @username, ссылки на
    профили (kwork.ru/user/*, t.me/*, .../resume/*). Имена людей заменяются на `[PERSON]`
    эвристикой по сигнальным фразам ("меня зовут", "заказчик:", ...) — **это не полноценный
    NER**, а точечная эвристика; имя без сигнальной фразы не поймается. Написан тест-первым
    (13 позитивных примеров), как требует ТЗ.
  - `src/export/exporter.py` + `/export [days]` — CSV (UTF-8 BOM для Excel) через
    `message.answer_document`, обезличенные поля, дубликаты кросспостов не экспортируются.
  - `src/export/stats.py` + `/stats [7d|30d]` — конверсия по источникам (собрано/уведомлено/
    ответил/выиграно), распределение бюджетов по корзинам.
  - `src/core/retry.py` (`retry_with_backoff`) — общий ретрай для вызовов без своего HTTP-слоя
    (Telegram Bot API через aiogram); применён к отправке лида, ежедневного брифа и алертов
    здоровья источников. У HTTP-источников уже был `src/core/http.py` с Фазы 1.
  - `src/notifier/health_alerts.py` + джоба в `main.py` (каждые 5 мин) — уведомление владельцу
    **сразу** при переходе источника в degraded (не только в ежедневном брифе), и при
    восстановлении; не спамит на каждый цикл (флаг в `system_state`).
  - `scripts/install_windows_task.ps1` — регистрирует автозапуск через Планировщик заданий
    Windows (старт при входе, до 5 перезапусков при сбое). `scripts/lead-radar.service` —
    шаблон systemd unit на случай переезда на Linux.
  - Все команды бота из ТЗ реализованы: `/brief /pause /resume /stats /export /addchat
    /sources /todo /done /snooze /health`.
  104 теста, `mypy --strict` чист.
  **Живой прогон нашёл ещё один баг**: `Settings` уже был пофиксен в Фазе 4 (пустые
  `TELEGRAM_API_ID`/`CHANNEL_ID`); в Фазе 5 новых живых багов не найдено — `/export` и `/stats`
  проверены только через unit-тесты с фейковыми `Message`, не через реальный Telegram (тот же
  сетевой блок `api.telegram.org`, что и в Фазах 2-3).
- **Постфактум (после Фазы 5, по прямой просьбе владельца):** `scripts/setup_bot.py` -
  разовая настройка профиля бота в Telegram (команды, описание, короткое описание) через
  `bot.set_my_commands/set_my_description/set_my_short_description`; ничего не отправляет в
  чаты, меняет только метаданные бота. Не может быть выполнен из песочницы (тот же сетевой блок
  `api.telegram.org`) - владельцу нужно запустить один раз. `README.md` в корне - пользовательская
  инструкция (в отличие от этого файла, который для меня). `setup.ps1`/`run.ps1` в корне -
  обёртки первого запуска и обычного запуска для человека без запоминания команд.
  107 тестов, `mypy --strict` чист.
- **Фаза 6 (по прямой просьбе владельца, после выхода изначального плана из ТЗ) — выполнена.**
  Четыре фичи: простой CRM, учёт дохода, библиотека шаблонов, отслеживание собственных откликов
  на hh.ru. Отдельно отклонена просьба владельца заставить юзербота самому подписаться на
  Telegram-каналы — прямое нарушение инварианта 3 и риск бана аккаунта; вместо этого список
  `telegram.chats` в `sources.yaml` заполнен 62 хендлами, собранными из трёх открытых статей
  (ссылки вытащены из реального DOM страниц через браузер, не через AI-пересказ — один
  источник с формулировками вроде `@backend_jobs`/`@ai_jobs` оказался переспамленной
  сгенерированной страницей с нулём настоящих ссылок и был отброшен целиком). Список
  непроверен вручную, отмечено прямо в комментарии над ним.
  - **CRM** (`src/crm/service.py`, миграция `0003_crm_income.sql` → таблица `companies`):
    напоминания о касаниях — не отдельный движок, а обычные строки `actions` с новым FK
    `company_id` — переиспользуют уже готовый бриф/эскалацию/`/snooze`/`/done`. Команды
    `/crm`, `/crm_add`, `/crm_touch`, `/crm_status`.
  - **Доход** (таблица `payments`, та же миграция): `/income`, `/goal`, цель месяца и % —
    новая секция в `compose_daily_brief`.
  - **Шаблоны** (`config/templates.yaml`, `src/control/templates.py`): статические тексты,
    `/templates` / `/template <имя>` — бот присылает текст, копирует и правит владелец,
    система по-прежнему никому не пишет сама (инвариант 1).
  - **Отклики на hh.ru** (`src/collectors/hh_applications.py`, миграция
    `0004_hh_applications.sql`): OAuth2 (authorization code + refresh), т.к. публичный API
    вакансий из Фазы 1 для чужих данных не подходит. `scripts/hh_oauth_login.py` — разовый
    интерактивный логин (открыть URL в браузере → вставить code → токены сами уйдут в `.env`
    через новый `src/core/env_file.py`, который точечно правит `.env`, не трогая остальное).
    `scripts/sync_hh_applications.py` сравнивает состояние с `last_notified_state` (не с
    текущим `state`), чтобы неудачная отправка уведомления не терялась молча — джоба в
    `main.py` (раз в 30 мин, только если все 4 переменные `HH_*` заполнены). Команда
    `/hh_status`. **Поля ответа `/negotiations` — по документации hh.ru, не проверены на живом
    ответе** (нет доступа к персональному OAuth-токену из песочницы) - если структура другая,
    `SourceUnavailableError` уведёт источник в degraded, не уронив систему, но сообщение будет
    неточным до ручной проверки владельцем.
  155 тестов, `mypy --strict` чист. Живых новых багов в этой фазе не найдено (тот же сетевой
  блок `api.telegram.org`/`hh.ru OAuth` для живой проверки, что и раньше).
- **Фаза 7 (по прямой просьбе владельца) — выполнена.** Настройки, которые раньше требовали
  правки `.env`/`keywords.yaml` и перезапуска, стали изменяемыми через бота на лету; плюс
  полноценное инлайн-меню вместо голых команд.
  - `src/core/runtime_settings.py` — `score_threshold`, `min_budget_rub`, `daily_brief_time`
    теперь читаются из `system_state` с фолбэком на `.env`/`keywords.yaml` (тот же паттерн,
    что уже использовался для `collecting_paused` и цели дохода). `apply_min_budget_override`
    подключён в `src/core/pipeline.py::score_and_store_lead` — единая точка для всех источников
    с реальными лидами, менять каждый коллектор по отдельности не пришлось.
  - Команды `/settings`, `/set_threshold`, `/set_budget_floor`, `/set_brief_time`. Смена
    времени брифа не просто пишет в БД — если бот запущен через `src/main.py`, `scheduler`
    прокидывается в хэндлеры через workflow data, и `/set_brief_time` вызывает
    `scheduler.reschedule_job("daily_brief", ...)` вживую, без перезапуска процесса.
  - **Инлайн-меню** (`src/control/menu.py`): `/start`/`/menu` — кнопки 📋 Бриф, 📊 Статистика,
    🏢 CRM, 💰 Доход, 📝 Шаблоны, 🔧 Источники, ⚙️ Настройки, ❓ Помощь; `/help` — тот же список
    текстом. Настройки внутри меню — не текстовый ввод, а пресет-кнопки (пороги 30/50/70/90,
    бюджет 3000/5000/10000/20000, время брифа 08:00-12:00), значение применяется сразу по тапу.
    Текстовые команды и кнопки меню используют ОДНИ И ТЕ ЖЕ функции (`bot.py`: `brief_text`,
    `stats_text`, `crm_text`, `income_text`, `templates_list_text`, `sources_text`,
    `settings_text` — без ведущего `_`, специально для переиспользования из `menu.py`), чтобы
    не разъезжались две копии одной и той же логики.
  - `menu.py` импортирует `bot.py` за этими функциями → сборку `Dispatcher` пришлось вынести в
    отдельный `src/control/dispatcher.py`, иначе получался цикл импортов (`bot.py` собирал бы
    диспетчер и импортировал `menu.py`, который импортирует `bot.py`). `main.py` импортирует
    `build_dispatcher` теперь оттуда.
  - `scripts/setup_bot.py::COMMANDS` пополнен, `test_setup_bot.py` сверяет список не с одним
    роутером, а со всеми `sub_routers` собранного `Dispatcher` — иначе тест не заметил бы
    команды, зарегистрированные в `menu.py`.
  179 тестов, `mypy --strict` чист.

## КОМАНДЫ БОТА

`/start` `/menu` `/help` `/brief` `/pause` `/resume` `/stats [7d|30d]` `/export [days]`
`/addchat <handle>` `/sources` `/health` `/todo <текст>` `/done <id>` `/snooze <id> <дней>`
`/crm` `/crm_add <название>` `/crm_touch <id> <дней> [результат]` `/crm_status <id> <статус>`
`/income <сумма> [заметка]` `/goal <сумма>` `/templates` `/template <имя>` `/hh_status`
`/settings` `/set_threshold <число>` `/set_budget_floor <число>` `/set_brief_time <ЧЧ:ММ>` —
плюс инлайн-меню (`/start`/`/menu`) и кнопки Открыть/Ответил/Мимо под каждым уведомлением о
лиде. Список синхронизирован с `scripts/setup_bot.py::COMMANDS`, тест `test_setup_bot.py`
сверяет его со всеми роутерами собранного `Dispatcher` (`src/control/dispatcher.py`).

## ЗАПУСК

Пользовательский путь (README.md) — `.\setup.ps1`, заполнить `.env`, `.\run.ps1` из корня
проекта. Ниже — эквивалентные команды напрямую:

Разовые прогоны источников (для проверки/наполнения БД до первого полного запуска):
```
python -m scripts.collect_hh
python -m scripts.collect_rss
python -m scripts.collect_kwork
python -m scripts.sync_hh_applications   # только если настроен HH OAuth
```
Разовый OAuth-логин hh.ru (перед первым sync_hh_applications):
```
python -m scripts.hh_oauth_login
```
Настройка профиля бота в Telegram (команды, описание, короткое описание — один раз):
```
python -m scripts.setup_bot
```
Полный процесс (планировщик + Telegram + бот) — после заполнения `.env`:
```
python -m src.main
```
Автозапуск: `scripts\install_windows_task.ps1` (Windows) или `scripts/lead-radar.service`
(Linux, шаблон). `setup.ps1`/`run.ps1` в корне — обёртки для человека, не трогать логику
внутри `scripts/` и `src/`.

### Что нужно заполнить в `.env` перед первым полным запуском
- `BOT_TOKEN` — уже заполнен.
- `TELEGRAM_API_ID` / `TELEGRAM_API_HASH` — my.telegram.org, для отдельной симки.
- `CHANNEL_ID` — создать приватный канал, добавить туда бота администратором, вписать id.
- Список Telegram-чатов в `config/sources.yaml` → `telegram.chats` — уже заполнен 62 хендлами
  из открытых источников (непроверенными вручную, см. комментарий в файле), почистить под
  себя, затем поставить `telegram.enabled: true`.
- Порог скоринга, минимальный бюджет, время брифа — стоят дефолты в `.env`/`keywords.yaml`,
  но менять их проще через бота: `/settings` или кнопка ⚙️ в `/menu` (Фаза 7), без правки
  файлов и перезапуска.
- Первый запуск Telethon потребует один раз интерактивно ввести код из Telegram — появится
  `*.session`, он в `.gitignore`.
- `HH_CLIENT_ID`/`HH_CLIENT_SECRET`/`HH_REDIRECT_URI` — опционально, только для `/hh_status`.
  Зарегистрировать приложение на dev.hh.ru, затем `python -m scripts.hh_oauth_login` сам
  впишет `HH_ACCESS_TOKEN`/`HH_REFRESH_TOKEN`.

Все прочие источники (`hh_ru`, `kwork_projects`, `kwork_catalog`, `rss_remote_jobs`) уже
`enabled: true` и не требуют дополнительной настройки.
